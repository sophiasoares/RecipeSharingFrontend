import { FormatIngredientsPipe } from './format-ingredients.pipe';

describe('FormatIngredientsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatIngredientsPipe();
    expect(pipe).toBeTruthy();
  });
});
